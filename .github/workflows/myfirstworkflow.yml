name: myfirstworkflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first job
        run: echo "hello"
      - name: print by
        run: echo "hai"
  
